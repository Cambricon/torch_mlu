## for pylint

please visit https://docs.pylint.org/en/1.6.0/features.html#messages for lint error info
if you want to ignore your lint error, you can add "# pylint: disable={,,,}" at the end of line where lint error happens
examples:

```python
from common_utils import testinfo, TestCase  # pylint: disable=C0413,W0110
```

## for cpplint

if you want to ignore your lint error, you can add "// NOLINT" at the end of line where lint error happens
examples:

```cpp
int n = 1;
int a[n];  // NOLINT
```
